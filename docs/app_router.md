Basic router mirrors this shape: 

POST   /users             create user
GET    /users             list users
GET    /users/{user_id}   get one user
PATCH  /users/{user_id}   update user
DELETE /users/{user_id}   delete/deactivate user
